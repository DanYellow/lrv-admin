{% block gallery_widget %}
{# {{ form.image}} #}
    {# {{ dump(form.image.vars.choices|json_encode()) }} #}
    {{ form_widget(form.image)}}
    {# <select {{ block('widget_attributes') }}></select> #}
{% endblock %}